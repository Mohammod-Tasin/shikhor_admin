# Base URL of the Go backend API (no trailing slash).
# The admin origin (default http://localhost:3001) must be present in the
# backend's ALLOWED_ORIGINS for CORS + login to succeed.
NEXT_PUBLIC_API_URL=http://localhost:8080
